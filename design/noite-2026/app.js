const app=document.querySelector('#app'),modal=document.querySelector('#modal');
const titles=[{id:'00',name:'Fallout',kind:'Série',duration:56,mood:'Explorar',reason:'Um mundo para explorar',left:41,progress:27},{id:'01',name:"Widow’s Bay",kind:'Série',duration:38,mood:'Surpreender',reason:'Mistério com humor',left:23,progress:40},{id:'02',name:'The Prestige',kind:'Filme',duration:130,mood:'Surpreender',reason:'Uma história cheia de pistas',left:72,progress:45},{id:'04',name:'From',kind:'Série',duration:51,mood:'Tensão',reason:'Mistério para a noite',left:32,progress:37},{id:'06',name:'Outcome',kind:'Filme',duration:84,mood:'Surpreender',reason:'Uma escolha em menos de 90 minutos'},{id:'10',name:'Spaceman',kind:'Filme',duration:108,mood:'Explorar',reason:'Ficção científica contemplativa'}];
let page='home',selected=titles[0],duration=999,mood='Todos',query='',duoIndex=0,votes=0,saved=new Set(),episodeTab='episodes';
const asset=t=>`assets/${t.id}.jpg`;
const card=(t,progress=false)=>`<button class="card" data-title="${t.id}"><div class="art" style="background-image:url('${asset(t)}')"><span class="time">${progress?t.left+' min restantes':t.duration+' min'}</span></div><strong>${t.name}</strong><small>${progress?'Retomar de onde parou':t.reason}</small>${progress?`<div class="progress"><i style="width:${t.progress}%"></i></div>`:''}</button>`;
function render(){const oldFocus=document.activeElement;const focusKey=oldFocus?.dataset?Object.entries(oldFocus.dataset)[0]:null;document.querySelectorAll('[data-page]').forEach(b=>{b.classList.toggle('active',b.dataset.page===page);b.setAttribute('aria-current',b.dataset.page===page?'page':'false')});
if(page==='home')app.innerHTML=`<section class="hero" style="background-image:url('assets/00.jpg')"><div class="hero-content"><p class="kicker">Sua próxima viagem já começou</p><h1 class="wordmark">FALLOUT</h1><div class="meta"><span>Temporada 1, episódio 1</span><span class="badge">14</span></div><p>O mundo lá fora mudou.<br>Continue explorando o que ficou.</p><div class="actions"><button class="primary" data-action="play">▶ Continuar · 41 min</button><button data-action="recap">Relembrar antes</button></div></div></section><section class="content"><div class="section-heading"><h2>De volta às suas histórias</h2><span class="quiet">Seu progresso, em um só lugar</span></div><div class="cards">${titles.slice(0,4).map(t=>card(t,true)).join('')}</div><div class="night-strip"><div><h2>Quanto tempo cabe na sua noite?</h2><p>Encontre uma história pelo tempo e pelo clima de hoje.</p></div><button data-page="discover">Escolher minha noite</button></div></section>`;
if(page==='discover')app.innerHTML=`<section class="page"><p class="kicker">Descobrir</p><h1>O que combina com hoje?</h1><p class="lead">Uma noite curta. Uma grande história. Comece pelo que você está a fim.</p><input class="search" aria-label="Buscar por título" placeholder="Buscar um título…" value="${query.replaceAll('&','&amp;').replaceAll('"','&quot;').replaceAll('<','&lt;')}"><div class="filters" aria-label="Tempo disponível">${[[999,'Sem pressa'],[45,'Até 45 min'],[90,'Até 90 min']].map(([v,l])=>`<button data-duration="${v}" aria-pressed="${duration===v}">${l}</button>`).join('')}</div><div class="filters" aria-label="Clima">${['Todos','Explorar','Surpreender','Tensão'].map(m=>`<button data-mood="${m}" aria-pressed="${mood===m}">${m}</button>`).join('')}</div><div class="section-heading"><h2>Suas possibilidades</h2><span class="quiet" id="result-count"></span></div><div class="cards" id="results"></div><div class="night-strip"><div><h2>“Algo misterioso, mas sem terror”</h2><p>Prévia da busca por intenção: refine com sugestões, sem digitar no controle.</p></div><button data-action="intent">Experimentar sugestão</button></div></section>`;
if(page==='detail')app.innerHTML=`<section class="hero" style="background-image:url('${asset(selected)}')"><div class="hero-content"><p class="kicker">${selected.reason}</p><h1>${selected.name}</h1><div class="meta"><span>${selected.kind}</span><span>${selected.duration} min</span><span class="badge">Dados do catálogo local</span></div><p>${selected.id==='00'?'Uma jovem deixa a segurança de um abrigo e descobre um mundo transformado.':'Uma nova história para a sua noite. Explore o título e escolha como assistir.'}</p><div class="actions"><button class="primary" data-action="play">▶ ${selected.left?'Continuar':'Assistir'}</button><button data-action="save">${saved.has(selected.id)?'✓ Na minha lista':'+ Minha lista'}</button><button data-action="sources">Áudio e fontes</button></div></div></section><section class="content"><div class="tabline"><button data-tab="episodes" aria-pressed="${episodeTab==='episodes'}">${selected.kind==='Série'?'Episódios':'Sobre o filme'}</button><button data-tab="related" aria-pressed="${episodeTab==='related'}">Na mesma atmosfera</button></div>${episodeTab==='related'?`<div class="cards">${titles.filter(t=>t.id!==selected.id).slice(0,4).map(t=>card(t)).join('')}</div>`:selected.kind==='Série'?`<div class="episode-row"><article class="episode"><span class="kicker">Seu episódio</span><h3>Continue de onde parou</h3><p>O foco volta ao episódio em andamento.</p><button data-action="play">▶ Retomar episódio</button></article><article class="episode"><span class="quiet">Próximo episódio</span><h3>A história continua</h3><p>Imagem e sinopse protegidas até você assistir.</p><button data-action="spoiler">Revelar prévia</button></article><article class="episode"><span class="quiet">Faz tempo que você não vê?</span><h3>Volte para a história</h3><p>Uma recapitulação limitada ao seu progresso.</p><button data-action="recap">Relembrar antes</button></article></div>`:`<div class="night-strip"><div><h2>Uma sessão sem interrupções</h2><p>Confira áudio, legendas e fonte antes de começar.</p></div><button data-action="sources">Ver opções</button></div>`}</section>`;
if(page==='together'){const t=titles[[2,1,5][duoIndex%3]];app.innerHTML=`<section class="page"><p class="kicker">Sessão em dupla</p><div class="duo"><div><h1>Dois gostos.<br>Uma boa escolha.</h1><p class="lead">Escolham juntos, com um controle só. Cada pessoa dá seu voto; os dois precisam topar.</p><div class="people"><span class="person ${votes===0?'selected':''}">H &nbsp; Henrique</span><span>+</span><span class="person ${votes===1?'selected':''}">C &nbsp; Convidado</span></div><p class="quiet">${votes===0?'Henrique, você assistiria?':'Convidado, agora é sua vez.'}</p><div class="vote"><button data-action="pass">Outra opção</button><button class="primary" data-action="vote">Eu topo</button></div><p class="quiet">Votos desta demonstração ficam apenas nesta página.</p></div><div><div class="feature-art" style="background-image:url('${asset(t)}')"><div class="overlay"><p>Uma possibilidade para vocês</p><h2>${t.name}</h2><p>${t.kind} &nbsp; ${t.duration} min</p></div></div><div class="why" style="margin-top:20px"><strong>Por que está aqui?</strong><p>${t.reason}. A proposta é mostrar o motivo da sugestão, sem porcentagens de afinidade inventadas.</p></div></div></div></section>`}
if(page==='player')app.innerHTML=`<section class="player" style="background-image:linear-gradient(0deg,#060c17 1%,#060c1750 60%),url('${asset(selected)}')"><div class="player-top"><button data-page="detail">‹ Voltar ao título</button><span class="note">Prévia estática do player</span></div><div class="controls"><span class="note">Seu filme continua aqui enquanto você ajusta as opções</span><h1>${selected.name}</h1><p>Reprodução demonstrativa pausada</p><div class="timeline"><i></i></div><div class="section-heading"><span>15:00</span><span>−41:00</span></div><div class="control-row"><div class="control-group"><button data-action="rewind">↶ 10 s</button><button data-action="toggle">▶ Reproduzir</button></div><div class="control-group"><button data-action="audio">Áudio e legendas</button><button data-action="recap">Onde parei?</button><button data-action="end">Ao terminar</button></div></div></div></section>`;
if(page==='discover')results();if(focusKey){const target=[...document.querySelectorAll('button')].find(b=>b.dataset[focusKey[0]]===focusKey[1]);target?.focus({preventScroll:true})}window.scrollTo(0,0)}
function results(){const found=titles.filter(t=>t.duration<=duration&&(mood==='Todos'||t.mood===mood)&&t.name.toLowerCase().includes(query.toLowerCase()));document.querySelector('#results').innerHTML=found.length?found.map(t=>card(t)).join(''):'<div class="empty">Nenhum título nesta combinação. Aumente o tempo ou escolha outro clima.</div>';document.querySelector('#result-count').textContent=found.length+(found.length===1?' opção':' opções');}
function show(title,body){document.querySelector('#modal-body').innerHTML=`<h2>${title}</h2>${body}`;modal.showModal();}
function go(p){page=p;location.hash=p;render()}
document.addEventListener('input',e=>{if(e.target.matches('.search')){query=e.target.value;results()}});
document.addEventListener('click',e=>{const b=e.target.closest('button');if(!b)return;if(b.matches('.close')){modal.close();return}if(b.dataset.page){go(b.dataset.page);return}if(b.dataset.title){selected=titles.find(t=>t.id===b.dataset.title);episodeTab='episodes';go('detail');return}if(b.dataset.duration){duration=+b.dataset.duration;render();return}if(b.dataset.mood){mood=b.dataset.mood;render();return}if(b.dataset.tab){episodeTab=b.dataset.tab;render();return}
switch(b.dataset.action){case'play':if(page==='home')selected=titles[0];go('player');break;case'save':saved.has(selected.id)?saved.delete(selected.id):saved.add(selected.id);render();break;case'recap':show('Relembrar, sem avançar na história',`<p>O resumo termina exatamente no último ponto assistido.</p><p class="quiet">Conceito: exige recaps revisados e vinculados ao episódio e ao tempo. Nenhum resumo está sendo gerado nesta demonstração.</p><div class="actions"><button class="close">Voltar à sessão</button></div>`);break;case'sources':show('Escolha como assistir','<p>As fontes e os idiomas serão confirmados pelo addon antes da reprodução.</p><div class="empty">Nenhuma fonte consultada neste mockup.</div>');break;case'spoiler':show('Prévia protegida','<p>Na versão final, esta ação revela a imagem e a sinopse do próximo episódio, após sua escolha explícita.</p>');break;case'intent':query='';duration=999;mood='Surpreender';render();show('Uma primeira direção','<p>A sugestão selecionou o clima “Surpreender”. Este protótipo usa filtros locais; interpretar uma frase livre é uma etapa futura.</p>');break;case'pass':duoIndex++;votes=0;render();break;case'vote':votes++;if(votes===2){selected=titles[[2,1,5][duoIndex%3]];votes=0;show('Deu match: '+selected.name,'<p>Os dois toparam esta história.</p><button class="primary" data-action="matched">Ver o título</button>')}else render();break;case'matched':modal.close();go('detail');break;case'audio':show('Sua forma de assistir','<p>Opções ilustrativas de apresentação.</p><div class="filters"><button data-action="option">Português</button><button data-action="option">Idioma original</button></div><p>Legendas</p><div class="filters"><button data-action="option">Português · maior</button><button data-action="option">Desativadas</button></div>');break;case'option':b.parentNode.querySelectorAll('button').forEach(x=>x.setAttribute('aria-pressed','false'));b.setAttribute('aria-pressed','true');break;case'end':show('Quando este episódio terminar','<div class="filters"><button data-action="option">Parar ao terminar</button><button data-action="option">Continuar a série</button></div><p>A escolha vale para esta sessão demonstrativa.</p>');break;case'toggle':show('Prévia do player','<p>Este estudo demonstra a interface. A reprodução real permanece no app nativo.</p>');break;case'rewind':show('Voltar 10 segundos','<p>No player nativo, este comando retorna dez segundos sem abrir uma janela.</p>');break;}});
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!modal.open){go('home');return}if(!['ArrowLeft','ArrowRight','ArrowUp','ArrowDown'].includes(e.key)||e.target.tagName==='INPUT')return;const root=modal.open?modal:document;const all=[...root.querySelectorAll('button,a,input')].filter(x=>x.getBoundingClientRect().width>0);const current=document.activeElement;if(!all.includes(current)){all[0]?.focus();e.preventDefault();return}const r=current.getBoundingClientRect(),cx=r.x+r.width/2,cy=r.y+r.height/2;const horiz=/Left|Right/.test(e.key),sign=/Right|Down/.test(e.key)?1:-1;const next=all.filter(x=>x!==current).map(x=>{let a=x.getBoundingClientRect(),dx=a.x+a.width/2-cx,dy=a.y+a.height/2-cy;return{x,forward:(horiz?dx:dy)*sign,side:Math.abs(horiz?dy:dx)}}).filter(x=>x.forward>5).sort((a,b)=>(a.forward+a.side*3)-(b.forward+b.side*3))[0];if(next){next.x.focus();e.preventDefault()}});
window.addEventListener('hashchange',()=>{let p=location.hash.slice(1);if(['home','discover','detail','together','player'].includes(p)&&p!==page){page=p;render()}});page=['home','discover','detail','together','player'].includes(location.hash.slice(1))?location.hash.slice(1):'home';render();
